version: '3.1'
services:
  web:
    image: registry.gitlab.com/seniorprojectntid/seniorproject:develop
    secrets:
      - sentry
    volumes:
      - type: bind
        source: ./logs
        target: /seniorproject/logs

secrets:
  sentry:
    file: ./.sentry_dsn
